describe( "home section", function() {
    beforeEach(module("jaws.home"));

    it("should set up the title", inject(function() {
        //expect($scope.title).toBe("JAWs");
        expect(true).toBeTruthy();
    }));
});
